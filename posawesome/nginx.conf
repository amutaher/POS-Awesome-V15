# Nginx configuration for POS Awesome
# Place this file in /etc/nginx/conf.d/

# SSL configuration
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
ssl_prefer_server_ciphers off;
ssl_session_timeout 1d;
ssl_session_cache shared:SSL:50m;
ssl_session_tickets off;
ssl_stapling on;
ssl_stapling_verify on;

# HSTS (uncomment if you're sure)
# add_header Strict-Transport-Security "max-age=63072000" always;

# Location blocks for POS Awesome
location /posawesome/ {
    proxy_pass http://127.0.0.1:8000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Feature-Policy "camera 'self'; microphone 'self'; geolocation 'self'" always;
    add_header Service-Worker-Allowed "/posawesome/" always;
    
    # Content Security Policy
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; font-src 'self'; connect-src 'self' https://*.farooqchemical.com; manifest-src 'self'; worker-src 'self'; frame-src 'self'; media-src 'self'; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests;" always;
}

# Service worker path
location = /posawesome/public/js/posapp/service-worker.js {
    add_header Service-Worker-Allowed "/posawesome/";
    add_header Cache-Control "no-cache";
    expires 0;
}

# Static files with caching
location /posawesome/public/ {
    expires 1y;
    add_header Cache-Control "public, no-transform";
}

# Manifest file
location = /posawesome/public/manifest.json {
    add_header Cache-Control "no-cache";
    expires 0;
}

# Deny access to sensitive files
location ~ /\. {
    deny all;
}

# Error pages
error_page 404 /posawesome/public/js/posapp/offline.html;
error_page 500 502 503 504 /50x.html; 